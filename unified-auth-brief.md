# 统一认证系统（auth.whleague.win）· 脑暴任务书

## 目标
设计 WHL 生态的统一认证系统（SSO），部署在 auth.whleague.win。本轮只出方案，不写代码。最终交付：
1. PRD：功能清单（每项标 P0/P1/P2）+ MVP 范围 + 砍掉什么
2. 技术方案：认证协议选型 + 中央账号模型 + 三系统迁移接入方案 + 数据模型草图

## 背景
- 代码主要由你写（vibecoding），我是需求方。术语用人话解释。
- 技术底座：Cloudflare 生态，与现有系统一致；域名 whleague.win。部署形态（纯 Workers / Pages）是开放议题，见问题 6。
- 生态现状：三个系统（赛事平台、竞猜系统、俱乐部平台）**已经上线在跑**，各有各的登录/绑定实现。本项目 = 新建 auth 身份中枢 + 把三系统迁移接入。
- 账号真源：**赛事平台的账号体系是源头**。QQ（经 AstrBot）和球队（俱乐部）都是绑定到中央账号的外部身份，不是账号本身。
- 注册开通：跟随赛事平台现有机制，不另造口子。
- 终态：WHL 生态所有涉及登录、认证、绑定的功能都收口到 SSO（含现有 AstrBot 插件的 web 部分），本期先把三系统接进来，数据模型要给后面留路。
- 三系统代码仓库在本机 `C:\Users\bhdjb\whlProgram\` 下（WHL-tournament-management-system / WHL-Daily-Activities-System / WHL-club-operations-platform）。

## 约束
- **这是迁移项目，不是绿地项目**：方案必须含双登录态过渡期和回滚路径，不许假设三系统停机改造。
- **先自查再发散**：先读三个仓库现有的登录/账号实现（若本地仓库是空壳，直接问我要实际部署代码的位置），摸清每个系统现在怎么存账号、怎么登录，再提方案。
- **auth 是安全件，错一处全线裸奔**：密码哈希、会话固定、CSRF、token 过期刷新、防暴力破解、QQ 绑定防冒充，每项都要有明确方案，写进验收。
- 联网调研 CF 生态做自建认证的成熟实践（Workers + D1 会话、OIDC 库、CF Access 适用边界），标来源；查不到标「假设」。
- 先发散后收敛 + 一次只问一个问题（见交互方式）。
- 技术选型每项两句话内说完：用什么 + 为什么 + 代价。

## 逐条回答这些问题
1. 认证协议怎么选：自建 OIDC/OAuth2 Provider、共享顶级域 Cookie+JWT、还是 CF Access？结合三系统都是 Pages+Workers 的现实给推荐，说清各自代价。
2. 中央账号模型怎么建：账号 / 凭证 / 绑定身份（QQ、球队）/ 会话 / 角色 的 ER？赛事平台的账号数据怎么搬——真源迁到 auth 后，赛事平台自己降级成 client，它的登录改造点在哪？
3. QQ 绑定管理：绑定/换绑/解绑流程怎么设计？怎么证明「这个 QQ 号是你的」（AstrBot 侧验证指令？）？一个 QQ 被绑到多个账号怎么处理？
4. 权限中心：角色是全局一套还是 auth 管全局角色、各系统留局部角色？三系统现有的管理员怎么迁？
5. 迁移与灰度：三个系统逐个列改造点清单；账号数据迁移怎么做？双登录过渡期怎么设计？出问题怎么回滚？
6. 部署形态：auth 用纯 Workers（Workers + Static Assets）还是 Pages？我倾向纯 Workers（直觉上更干净、路由和中间件更灵活、CF 也在把两条产品线收敛到 Workers），但你要拿事实确认：auth 作为登录网关对路由/中间件的需求、两侧配额与定价差异、迁移成本，分析后给明确结论。

## 验收标准
- 功能覆盖：注册承接 → 登录 → 绑定/换绑/解绑 → 会话管理 → 全局登出 → 权限管理 → 三系统迁移，每项标 P0/P1/P2。
- P0 是最小闭环：赛事平台账号迁入 auth → 在任一系统登录跳 auth 回来 → 三系统登录态全通 → QQ 绑定/换绑走 auth → 全局登出生效。
- 安全清单逐项有方案：密码哈希 / CSRF / 会话固定 / 防爆破 / QQ 绑定防冒充。
- 迁移方案含双登录过渡期与回滚路径，不是只画 happy path。
- **「个人资料统一收编 auth」单列一节影响分析**：好处 / 坏处 / 你的建议，供我决策（这是开放议题，不许默认收编）。
- 数据模型草图 + 登录全流程时序图（含未登录跳转、callback、会话过期）。
- 结论先行：先给推荐方案，发散过程和对比细节放后面。

## 交互方式（多轮会话）
- 第 1 轮：先读三系统现有登录实现 + 本任务书，然后只发散 + 只提问。先给切入方向，并列出你全部想问的问题（编号清单，标出哪个最关键），先别收敛。
- 提问节奏：一次只问一个，从最要命的开始，每问带一句"为什么问这个"。我可以说「跳过」或「你标假设」，不许卡死在某一题。
- 问完（或我说够了）：收敛成 PRD + 技术方案初稿。
- 我确认后：落成 PRD.md 和 TECH_DESIGN.md 存进工作区。
