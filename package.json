{
  "name": "whl-auth",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8792",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply whl-auth --local",
    "db:migrate:remote": "wrangler d1 migrations apply whl-auth --remote"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^6.2.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
