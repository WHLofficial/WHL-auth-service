# WHL-auth-service · 严格测试任务书

## 目标
对本地仓库 C:\Users\bhdjb\whlProgram\WHL-auth-service 的统一认证服务做一轮详细、完备、严格的测试：找出所有功能与安全缺陷，修复高危项，并把可自动化的用例固化成回归测试。交付三样东西：
1. 测试发现清单（严重度排序，每条带证据和复现步骤）
2. 高危项修复 + 修复后复测记录
3. 可一键回归的测试套件（`npm run test` 全绿）

## 背景
- 技术栈：Hono + jose(JWT) + D1 + KV，TypeScript，wrangler dev 端口 8792。先读 PRD.md、TECH_DESIGN.md 再看 src/，TECH_DESIGN 里的安全设计就是测试的对照标准——测「实现了设计没有」。
- 项目现状：本地 main 领先 origin 31 个提交（未 push）；wrangler.jsonc 里 D1 和 RL_KV 还是占位 ID，**线上没有真实资源，本服务未部署**。会话通过 SESSION_KV 与 tour/guess 共享（sess:* 兼容键）。
- 安全底线（PRD/任务书既定）：密码哈希、会话固定、CSRF、token 过期刷新、防暴力破解、QQ 绑定防冒充——这六项是必测项。
- 本会话权限假设：你可以装 dev 依赖（如 vitest）、跑本地迁移、起本地实例。

## 约束（红线，逐条遵守）
- **本地沙盒**：只跑 `npm run db:migrate:local` 和 `npm run dev`。禁止：`db:migrate:remote`、`wrangler deploy`、操作任何线上 D1/KV 资源、`git push`。.dev.vars 是本地密钥，不提交不外传。
- **证伪导向**：测试是主动构造攻击和边界，不是跑通 happy path 就收工。不预设实现正确。
- **每个发现三件套**：严重度（高/中/低）+ 证据（文件:行号，或完整请求/响应）+ 复现步骤（curl 命令或脚本）。「感觉有问题」不算发现。
- **修复分级**：高危（可被外部利用/可越权/可绕过认证）直接修，修完必须复测原用例并确认没修出新问题；中低危列清单等我确认。修复优先最小改动，新增依赖要说明理由。
- 规则或文档没覆盖的地方标「假设:xxx」，不许编。
- 环境可自查的先自查（路由、配置、迁移脚本、TECH_DESIGN 章节号），查不到再问我。

## 测试清单（逐项给结论：防住了 / 没防住 / 不适用+理由）

### A. 功能闭环（正例 + 反例都要）
1. 注册承接 → 登录 → 会话建立 → 访问受保护资源 → 全局登出，全链路走通。
2. 登录反例：错误密码、不存在账号、被禁用账号——响应与耗时是否泄露信息。
3. QQ 绑定/换绑/解绑：重复绑定、解绑后旧会话是否存活、换绑窗口期劫持可能。
4. 会话：过期刷新、登出后会话在 KV 是否真删除、并发会话行为。
5. 参数边界：所有表单/JSON 字段的缺失、超长、类型错误、Unicode。

### B. 安全专项（渗透式）
1. 密码存储：查 D1 确认非明文，评估哈希算法与参数强度。
2. CSRF：所有状态变更接口（绑定/解绑/登出/改密），构造跨站请求验证，查 Cookie SameSite/HttpOnly/Secure 属性。
3. 会话固定：登录成功前后 session id 是否轮换。
4. 防爆破：连续错误登录触发 RL_KV 限流？尝试绕过（伪造 X-Forwarded-For 等头、分布式键）。
5. QQ 绑定防冒充：验证流程能否被伪造或重放；绑定请求能否替别人发起。
6. 越权：水平（用户 A 操作用户 B 的绑定/会话/资料）、垂直（普通用户打管理员接口）。
7. JWT/session 实现：jose 用法（算法白名单、密钥强度）、token 过期与撤销链路——登出后旧 JWT 还能不能用。
8. 注入与输入：D1 是否全程参数化；重定向（登录后回跳 URL）能否被用来做开放重定向钓鱼。
9. 逻辑漏洞：竞态（并发绑定/并发登录）、重放（回调 code 复用）、流程跳步（不登录直接打后续接口）。

### C. 三系统 SSO 联调（本地）
1. 按 TECH_DESIGN 的兼容桥设计，本地起 tour（起不来就降级为兼容桥逻辑的代码级验证 + 集成测试，如实标注没验证到什么）。
2. 验证：tour 侧未登录跳 auth → 登录回跳 → sess:* 兼容键互通 → auth 全局登出对 tour 会话生效。
3. 回跳 URL 校验：白名单外域名能否通过回跳参数注入。

## 流程
1. 读 PRD / TECH_DESIGN / src，产出「测试面清单」：全部路由、全部状态变更点、全部 KV/D1 读写点。
2. 起本地环境（本地迁移 + dev），冒烟确认服务能跑。
3. 按 A → B → C 顺序执行，边测边记发现，别攒到最后。
4. 汇总发现清单（严重度排序）→ 修复高危 → 复测 → 固化回归测试。
5. 最终报告（结论先行）落盘 TEST_REPORT.md：发现清单、修复记录、测试覆盖说明（哪些测了、哪些没测到、为什么）。

## 验收标准
- 六项安全底线每项有明确结论，A/B/C 清单无遗漏项——不适用也要给理由。
- 每条发现：严重度 + 证据 + 可执行的复现步骤。
- 修复有记录：改了什么文件、为什么这么修、复测结果。
- `npm run test` 一键回归全绿；测试覆盖核心闭环路径与全部高危修复对应的用例。不强求覆盖率数字。
- 测试过程自主推进；只有环境起不来、或发现与 PRD/TECH_DESIGN 冲突需要裁决时才问我，一次一个问题。
