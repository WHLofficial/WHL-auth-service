# WHL 统一认证系统（auth.whleague.win）· PRD

> 版本：v1.1（2026-09-18，增量 8） · 状态：P0 主体已上线并收口（四系统 2026-09-14 全量切 OIDC）；P1 部分交付（见各行标注）
> 配套文档：[TECH_DESIGN.md](./TECH_DESIGN.md)（协议选型、数据模型、迁移与安全方案）

## 1. 概述

WHL 生态现有三个系统（赛事平台 tour.whleague.win、竞猜系统 guess.whleague.win、俱乐部平台 club.whleague.win）共享一套「隐式 SSO」：主域 cookie + 共享 KV 会话，账号真源在赛事平台。本项目新建 **auth 身份中枢**（auth.whleague.win），把账号、凭证、QQ 等外部身份、会话、权限全部收口到 auth，三个系统改造为标准客户端（client）接入。

**本期目标**：完成 P0 最小闭环——三系统登录态全通、QQ 绑定走 auth、全局登出生效、账号真源切换至 auth。AstrBot 插件群（积分、成长控制台、谈判）的收口只做本期必需的部分（绑定指令改调 auth），其余留 P2。

## 2. 术语表（人话版）

| 术语 | 人话解释 |
|------|----------|
| SSO 单点登录 | 登录一次，所有系统都认。由 auth 统一发「已登录」的身份 |
| OIDC 授权码流程 | 你访问竞猜站没登录 → 跳到 auth 的登录页 → 登录成功后 auth 发一张「一次性票据」把你送回竞猜站 → 竞猜站拿票据向 auth 换你的身份信息。全程票据只能用一次，别人捡到也没用 |
| PKCE | 上面流程的防截胡加固：竞猜站发起跳转前先发一个「暗号」，换票据时必须对上暗号，防止授权码被中间人截走 |
| client（客户端） | 接入 SSO 的业务系统。club/guess/tour 都是 auth 的 client |
| identity（外部身份） | 绑到中央账号上的外部标识，比如你的 QQ 号。账号是本体，QQ 是挂在账号上的一个身份 |
| 权限点 | 具体到某件事的权限，比如「竞猜-开盘结算」「俱乐部-导入球员」。角色 = 一组权限点 |
| refresh token | 长效「续命券」：登录态快过期时用它换新票据，不用重新输密码。只能用一次，重复使用说明被偷了，auth 会立刻作废整族会话 |
| back-channel 登出 | 全局登出时，auth 服务器对服务器地通知每个 client「把他的本地登录态清掉」，用户浏览器不用打开每个系统挨个登 |
| 兼容会话桥（过渡形态） | auth 迁移第一阶段临时按老格式（主域 cookie + KV）签发登录态，让三个系统一行代码不改就先「接入」 |
| 观众号（locked） | 开放注册产生的受限账号，解锁前不能绑队。锁定状态是账号属性，不是封禁 |
| must_change_pw | 管理员重置密码后强制用户下次登录改密的状态位 |

## 3. 已拍板决策记录（2026-09-12 脑暴会）

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 过渡策略 | auth 先接管现有共享会话机制（零改动接入），再逐系统切 OIDC，最后退役共享基建 |
| 2 | AstrBot 插件 | 源码在本机（`C:\Users\bhdjb\whlPointSystem\AstrBot\data\plugins\`）可改；QQ 绑定收口进 P0 |
| 3 | 规模/预算 | ≤50 注册用户，CF Free 套餐。方案按 Free 设计；Workers Paid（$5/月）列为可选加固（收益：密码哈希迭代数可大幅提升） |
| 4 | 注册入口 | 跟随各系统切换节奏收敛，过渡期新旧注册入口并存（数据写同一张表，无害） |
| 5 | 角色体系 | **完整权限点模型**：auth 定义权限点，角色是权限点集合，三系统判定改权限点校验。上线时按「行为等价」播种，避免管理员判定第一天变样 |
| 6 | 俱乐部平台 | 未正式上线 → 直接按 OIDC 接入，当迁移试点；域名规划 club.whleague.win |
| 7 | 节奏 | 不设时间盒，质量优先；灰度与回滚演练纳入验收 |
| 8 | 状态机制 | locked / must_change_pw / 邀请码注册，auth 全盘继承 |
| 9 | 个人资料收编 | 中立，待影响分析拍板（见 §7，本期默认不收编） |
| 10 | 安全增强 | 2FA、邮箱验证等全标 P2 本期不做；硬安全五项（密码哈希/CSRF/会话固定/防爆破/QQ 防冒充）进 P0 |
| 11 | 管理台归属（增量 8，2026-09-18） | **界面继续留在 tour 管理台，auth 只出能力**（12 条 HMAC 机器端点）。理由：管理台依赖 client 的权限点做门禁，迁到 auth 需先解决「谁在 admin 端点做鉴权」，属下一次全面重构的议题；本轮是止血（tour 侧 6 处账号写操作指向自己已归档的 user 表，管理员日常入口批量失效）。auth 原生管理界面留作后续 |
| 12 | 授权编辑粒度（增量 8） | 账号管理台按 **app 分组勾角色**（多选叠加）+ 权限点明细只读 + **权限点「额外授予」**（只加不减，落在新表 `account_permission`）。理由：迁移后一个账号常挂 5 行 user_role，单角色二选一既看不到也改不了 guess/club 侧角色；但账号级「减权」会让增减优先级变复杂且权限点英文对管理员不友好，全权限点化留给下一次全面重构 |
| 13 | 停用语义（增量 8） | **单开 `account.disabled_at`**，`locked`（观众号）语义完全不动。理由：`locked` 不是封禁——它只挡「绑队/提交阵容」，登录与首页照常（`tests/e2e/register.test.mjs` 有断言）；复用它会「停不住人」，给它补登录拦截又会打死观众号概念 |
| 14 | 会话管理（增量 8） | 账号详情可看**活跃会话**（登录 IP/登录时间/最后活跃/过期）+ 单个强制下线；**重置密码与停用自动吊销该账号全部会话**并 back-channel 通知各 client |

## 4. 功能清单

### P0（MVP 最小闭环）

| # | 功能 | 说明 | 验收标准 |
|---|------|------|----------|
| P0-1 | 注册承接 | 邀请码注册 + 开放注册（产 locked 观众号），机制照搬赛事平台，auth 托管注册页。过渡期写赛事库，收口后写 auth 库 | 凭有效邀请码在 auth 完成注册并自动登录；过期/超次邀请码被拒；开放注册产物为 locked |
| P0-2 | 登录 | 昵称 + 密码；locked / must_change_pw 拦截语义全盘继承；登录/注册双限流 | 正确凭据登录成功且三系统可用；错误凭据触发限流；locked 提示受限、must_change_pw 强制改密 |
| P0-3 | 兼容会话签发（迁移第 1 步） | auth 按现有格式写 KV `sess:{token}` + 种主域 cookie `whl_session` | auth 登录后，tour/guess/club 在**零代码改动**下读到登录态 |
| P0-4 | OIDC Provider 核心 | authorize / token / userinfo / jwks / revoke；授权码 + PKCE + refresh token 轮换 + back-channel 登出 | 标准 OIDC 流程可跑通；code 一次性且 ≤60s；refresh token 重用触发整族吊销 |
| P0-5 | club 接入（试点） | 俱乐部平台首次部署即按 OIDC 接入（无历史登录代码，零迁移负担） | club 登录跳 auth 回来；全局登出在 club 生效 |
| P0-6 | guess 接入（最重） | 登录/注册入口指向 auth；直写赛事库的代码下线；本地 30 天会话退役；QQ 绑定收口 | guess 登录跳 auth 回来；预测/管理台功能正常；发奖同步（qq_id 关联）不中断 |
| P0-7 | tour 接入（降级） | 登录页跳 auth；会话中间件改 OIDC；`/api/auth/*` 退役 | tour 登录跳 auth 回来；教练绑队/管理台正常 |
| P0-8 | QQ 绑定/解绑/换绑 | auth 页生成一次性码（10 分钟）→ QQ 群「绑定 <码>」→ 插件 HMAC 调 auth 写 identity；解绑需 QQ 侧指令确认；换绑 = 解绑 + 重绑 | 绑定/解绑/换绑全程走 auth；一个 QQ 无法绑第二个账号；冒用他人码无法绑定 |
| P0-9 | 全局登出 | auth 吊销会话 + 吊销其签发的全部 token + back-channel 通知各 client；兼容期删 KV 键即刻全端失效 | 任一系统登出后，三系统 + auth 全部回到未登录态 |
| P0-10 | 权限点模型落地 | 权限目录 + 角色 + 授权表；按行为等价播种（映射表见 TECH_DESIGN §6）；三系统判定改 `requirePermission()` | 播种后各系统管理员的可见/可用范围与迁移前**完全一致**；越权访问被拒并记审计 |
| P0-11 | 账号真源切换 | tour user 表 → auth account 一次性迁移 + 校验；tour user 表转只读 | 迁移前后账号数一致、抽样登录验证通过；校验不过不切读 |
| P0-12 | 安全五项 + 审计最小集 | 见 TECH_DESIGN §8；audit_log 记录登录成败/绑定变更/授权变更/吊销 | 安全清单逐项落地；关键事件可查。**增量 8 补齐审计缺口**：新增角色/权限授予与回收、会话吊销、重置密码、停用/启用、解锁、注册码生成、开放注册开关共 11 类事件，§8.8 要求的 role.grant/role.revoke/session.revoke 三类首次有了产生入口 |
| P0-13 | 迁移工具与回滚 | 每系统 compat 开关、迁移校验脚本、回滚 runbook | 演练：任一阶段按 runbook 可在 10 分钟内回滚 |
| P0-14 | 球队绑定上收（增量 7） | 球队绑定真源上收 auth：0008 新表 team/team_bind_code/team_binding；机器端点五条 HMAC（发码/烧码/解绑/登记/关联，密钥共用 BIND_SECRET）；tour/club 双入口发码烧码写同一张中央表，两侧旧绑定表休眠、经只读 AUTH_DB 派生读；存量以 tour team_member 为基准迁移（`scripts/migrate-team-bindings.mjs`，冲突/单边人工裁决） | tour 与 club 绑定/发码互通且指向同一绑定关系；一账号一队约束全生态生效；烧码原子不重放；迁移脚本幂等且冲突清单完整 |

### P1

| # | 功能 | 说明 |
|---|------|------|
| P1-1 | auth 管理台 | 账号列表 / 重置密码（生成临时密码 + must_change_pw）/ 解锁 / 角色授权编辑 / 注册码生成——接管 tour admin 的账号管理职能（真源收口的前置条件）。**增量 8 已落地能力层**：auth 新增 12 条 HMAC 管理机器端点（`/api/admin/*`），tour 管理台（`src/pages/Accounts.tsx`、`AdminCodes.tsx`）改为转发调用，原先 6 处写自己 user 表的死写全部改道；界面仍在 tour，auth 原生界面未做 |
| P1-2 | 用户自助会话管理 | 查看自己的活跃会话（设备/时间），单个吊销。**增量 8 落地管理侧、增量 10 落地自助侧（已完成）**：auth 原生 `/sessions` 页列出自己的活跃会话（IP / 登录时间 / 最后活跃 / 过期 / 当前设备标记），支持单个下线与「下线其他设备」（保留本机）；吊销与审计同 batch、联动 refresh 吊销与 back-channel 通知 |
| P1-3 | audit_log 查询界面 | 按账号/事件类型/时间筛选。**增量 10 已完成**：auth 新增 `POST /api/admin/audit/query`（id 倒序游标分页、单批 ≤100、0010 补 idx_audit_event），界面在 tour 管理后台 `/admin/audit`（超管专属，P1-1 既定「界面在 tour」） |
| P1-4 | 换绑 UX 完善 | auth 页面发起解绑 → QQ 确认指令的完整双向流程与提示。未做（QQ 侧「解绑」指令已在插件落地，缺 auth 页发起这一半） |

### P2（本期砍掉，预留扩展位）

| # | 功能 | 说明 |
|---|------|------|
| P2-1 | 2FA（TOTP）/ 邮箱验证 / 异地登录提醒 | credential.type 已预留第二因子扩展位 |
| P2-2 | 个人资料收编 | 待 §7 影响分析拍板，本期默认不收编 |
| P2-3 | whleague.win 根域门户页 | 生态入口 + 登录状态展示；club 前端指向根域的死链本期先改指 auth |
| P2-4 | AstrBot 成长控制台 SSO 收口 | 该控制台目前靠 AstrBot Dashboard 登录态保护、无端点级权限，属后续对接 |
| P2-5 | 谈判插件 QQ→球队绑定收口 | 由 `identity(QQ) → 账号 → 俱乐部绑定` 链推导，数据模型已留路 |
| P2-6 | 第三方 OAuth（微信/GitHub 等）、手机号注册 | identity.provider 可扩展 |
| P2-7 | organization 子账号/团队体系 | tour 的 organization 表继续兼容 |

**永久排除**：CF Access（机制不适配，理由见 TECH_DESIGN §3）；共享 Cookie+JWT 作为终态方案。

## 5. MVP 范围与总验收

**MVP = P0 全部 13 项。** 总验收链路（对应任务书 P0 闭环）：

1. auth 上线并完成账号迁移（真源收口后 tour user 表只读）；
2. 在 club / guess / tour 任一系统点登录 → 跳 auth → 登录成功 → 回到原系统，登录态生效；
3. 登录任一系统后打开另两个系统 → 均已登录（单点登录生效）；
4. 在 auth 完成 QQ 绑定 → 竞猜站发奖链路正常（qq_id 关联不断）；
5. 解绑 + 换绑全程走 auth，积分余额不受影响；
6. 任一系统点全局登出 → 三系统 + auth 全部回到未登录态；
7. 播种后的权限判定与迁移前行为一致；
8. 安全清单（TECH_DESIGN §8）逐项可验证；
9. 回滚演练通过（任一阶段 10 分钟内可回退）。

## 6. 核心用户旅程

**新教练入驻**：管理员生成邀请码 → 教练在 auth 注册（自动登录）→ 在 tour 或 club 任一侧凭球队认证码绑队（同一张 auth 中央表，两端互通）→ 打开竞猜站已是登录态 → 绑 QQ 后参与竞猜，赢得积分自动入 QQ 钱包。

**日常登录**：用户打开任意系统 → 未登录则跳 auth → 登录一次 → 其余系统静默登录（auth 会话 7 天内免重复输密码）。

**换 QQ 号**：用户在 QQ 群发「/解绑」（或 auth 页发起 + QQ 确认）→ 解除旧 QQ → 在 auth 页生成新码 → 新 QQ 发「绑定 <码>」→ 完成。竞猜积分在 QQ 侧不受影响。

**管理员重置密码**：P1 前：tour admin 现有页面（直写 user 表）；P1 后：auth 管理台。生成临时密码 + 强制改密语义不变。

## 7. 开放议题：个人资料统一收编 auth（待拍板）

**现状**：全生态用户资料 = 昵称 + 一个可选、未验证、不可修改的邮箱；无头像；三个系统都没有资料编辑页。

**好处**：① 未来统一门户/个人中心一处展示；② AstrBot web、未来新系统直接从 OIDC claims 取资料，不重复建表；③ 将来上头像/签名时只有一个权威口子。

**坏处**：① auth 从纯安全件变成业务数据持有方，备份/隐私/审计面变大；② 昵称兼登录名，收编后「改昵称」牵出登录名/显示名拆分的隐藏工作量；③ 当前资料趋近于零、无编辑入口，现实收益≈0，唯一受益方（门户）本身是 P2。

**技术侧建议**：本期不收编。auth 保持窄面；数据模型已按可扩展设计（将来加 profile 表 + OIDC `profile` scope 即可），将来收编无迁移阻力。**待需求方拍板。**
